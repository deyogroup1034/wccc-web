# Dev-only overrides, loaded by `next dev` but never by `next build`.
# Don't put NEXT_PUBLIC_SITE_URL in .env.local — that file is loaded during
# production builds too, and a localhost value there leaks into the deployed
# canonical/og:url/sitemap URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
